This code reads the pubmed articles in xml format, and then analyze the country of authors
## Get Started
First, download the `pmc_result.xml` file and put it under [`data/`](data/) folder.

Then, execute 
```bash
python preprocess.py
```